type Proposal @entity {
  id: ID!
  blocktime: BigInt!
  votes: [Votes!]! @derivedFrom(field: "proposalID")
}

type Votes @entity {
  id: ID!
  voter: Bytes
  single_vote: BigInt!
  proposalID: Proposal! 
}

type Implementation @entity{
  id: ID!
  blocktime: BigInt!
  newImplementation: Bytes
}
